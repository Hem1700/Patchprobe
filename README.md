# PatchProbe

CLI-first tool for defender-focused binary patch diffing with LLM-assisted triage.

This repository contains:
- `patchdiff` CLI skeleton
- schema definitions in `specs/schemas/`
- a detailed implementation document

See `Implementation_Doc.md` for full details.
